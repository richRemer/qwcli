const head = Symbol("CLI.head");
const lead = Symbol("CLI.lead");
const rest = Symbol("CLI.rest");
const term = Symbol("CLI.term");

const stateful = Symbol("CLI.stateful");

module.exports = {
    head: head,
    lead: lead,
    rest: rest,
    term: term,

    stateful: stateful
}
